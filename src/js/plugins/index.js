/**
 * @module js.plugins.index
 * @description Entry point for Deck system plugins and services.
 * 				Plugins are initialized globally via deck.use() during the boot sequence.
 */

// import Analytics from "./analytics.js";

/**
 * You can export an Array of plugin classes/instances, 
 * or an Object keyed by plugin name.
 */
export default [
	// Analytics,
];